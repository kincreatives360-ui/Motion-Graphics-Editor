{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/motion-graphics-editor",
    "build": "npm run build --workspace=@workspace/motion-graphics-editor",
    "lint": "tsc --build --noEmit",
    "typecheck": "tsc --build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
